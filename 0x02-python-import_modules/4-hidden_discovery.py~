#!/usr/bin/python3

if __name__ == "__main__":
    from sys import argv

    sum = 0
    for arg in range(len(argv) - 1):
        sum += int(argv[arg + 1])

    print("{}".format(sum))

    #print(sum(int(arguments) for arguments in argv[1:]))
